https://github.com/nikic/FastRoute/tree/master

-создаем переменную $router
-присваеваем ей произведение функкции simpleDispatcher
-аргументом simpleDispatcher будет анонимная функция
-аргументом анонимной функкции будет обьект new Router
    проверит наличие кеша
    вернет кеш либо
    функкция будет поочередно присваивать роуты обьекту роутер
    вернет обьект роутер
-у обьекта роутер вызовем метод диспатч

роутер будет состоять из routeParser и Dispatcher
    роутеру нужо передать роутерКоллектор который хранит роуты


Router при команде addRoute
    проверяет наличие кеша
        если нет кеша берет массив роутов
        и просчитывает их каждый
        если есть кеш то помешает его в RouteCollectorColector
        

    принимает path и пропускает его через функцию parse
    обьекта RouteParser
    RouteParser возвращает массив параметров и для сравнения
    пример: www.game.com/user/{id} = [1=>user,2=>id]
    пример: www.game.com/user/{id?} = [1=>user,2=>id:optional?]
    
    следущим этапом роутер добавляет в RouteCollector обьект
        Route(pmethod.path,handler)
            staticVars=>/user/{id} 
            parserVars=>[1=>user,2=>id]

    возвращает войд

Router при команде dispatch
    принимает method и uri
